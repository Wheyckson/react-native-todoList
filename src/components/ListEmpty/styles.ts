import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    marginTop: 32,
    justifyContent: "center",
    alignItems: "center",
  },

  imgEmpty: {
    marginBottom: 24,
  },

  labelLight: {
    fontSize: 14,
    color: "#808080",
  },

  labelDark: {
    fontSize: 14,
    color: "#333333",
  },
});
